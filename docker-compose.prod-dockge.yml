version: "3.8"

services:
  api:
    image: tm4soul.ch/tm4soulch-api::0.1.1
    ports:
      - "8911:8911"
    depends_on:
      - db
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SESSION_SECRET=${SESSION_SECRET}

  web:
    image: tm4soul.ch/tm4soulch-web::0.1.1
    ports:
      - "8910:8910"
    depends_on:
      - api
    environment:
      - API_HOST=http://api:8911

  db:
    image: postgres:16-bookworm
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - ./postgres:/var/lib/postgresql/data

  console:
    user: root
    image: tm4soul.ch/tm4soulch-console:0.1.1
    tmpfs:
      - /tmp
    command: "true"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      - db
